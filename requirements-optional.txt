# Installed by the full installer; each one is optional and degrades gracefully.
onnx-asr>=0.6.0           # Parakeet TDT (low-latency dictation)
onnxruntime>=1.18         # CPU/DirectML execution provider for Parakeet
reportlab>=4.1            # PDF export of meeting notes
soundfile>=0.12           # transcribe .mp3/.m4a/.flac from the CLI
